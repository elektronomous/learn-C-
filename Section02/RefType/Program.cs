using System;

namespace RefType
{
    class Program
    {
        static void Main(string[] args)
        {
            /* 
                = Reference type is a variable type which instead of storing the value in memory directly,
                  stores the memory location of the actual data.
                = The variable here stores the memory reference of the data and not the data directly
                = Reference type data types are string, class, Array etc.
                = When we copy this reference type of a data type it will just copy the memory address of the data
                  so we will then have two variable pointing to the same data.

            */
        }
    }
}
