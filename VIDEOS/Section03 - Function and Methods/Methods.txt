= A method is a code block that contains a series of statements.
= A program causes the statements to be executed by calling the method and specifying
  any required method arguments.
= In C#, every executed instruction is performed in the context of a method.
= The main method is entry point of every C# application and it is called by the CLR(
    common language runtime) when the program is started